# 构建产物（本地编译的二进制/压缩包均不上传，发布资产由 CI 生成）
*.exe
*.zip
*.dmg
*.msi
*.pkg
*.7z

# 本地配置（端口 / 目录）
config.json

# 运行时/测试日志
plugin-update.log
*.log

# 系统 / 编辑器
Thumbs.db
.DS_Store

# 图标/网站预览物料（不提交）
docs/preview*.png

# Wails 构建产物（src/build/bin）与自动生成的绑定（src/frontend/wailsjs）
src/build/bin/
src/frontend/wailsjs/

# 本地部署用 icns（scripts/restyle-appicon.mjs 生成，wails build 会从 src/build/appicon.png 自动重生成）
docs/iconfile.icns
